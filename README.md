# OneTimeSecret API

Сервис для создания и чтения одноразовых секретов с шифрованием. После первого прочтения секрет автоматически удаляется.

## 🚀 Технологии

- **FastAPI** - современный веб-фреймворк для Python
- **MongoDB** - база данных для хранения секретов
- **PyMongo** - драйвер для работы с MongoDB
- **Cryptography** - шифрование данных (Fernet)
- **JWT** - аутентификация (опционально)
- **Docker** - контейнеризация

## 📋 Требования

- Python 3.11+
- MongoDB 6.0+
- pip

## 🔧 Установка

### Локальная установка

1. **Клонировать репозиторий**
   ```bash
   git clone https://github.com/yourusername/key_manager.git
   cd key_manager
   ```

2. **Создать виртуальное окружение**
   ```bash
   python -m venv venv
   # Windows
   venv\Scripts\activate
   # Linux/Mac
   source venv/bin/activate
   ```

3. **Установить зависимости**
   ```bash
   pip install -r requirements.txt
   ```

4. **Настроить переменные окружения**
   ```bash
   cp .env.example .env
   # Отредактируйте .env файл и укажите свои значения
   ```

5. **Запустить MongoDB**
   ```bash
   # Через Docker
   docker run -d -p 27017:27017 --name mongodb mongo:6
   
   # Или установите MongoDB локально
   ```

6. **Запустить приложение**
   ```bash
   python src/main.py
   ```

   Приложение будет доступно по адресу: http://localhost:8000

### Docker Compose

```bash
docker-compose up -d
```

## 📚 API Endpoints

### Создание секрета
```http
POST /api/v1/generate
Content-Type: application/json

{
  "secret": "Мой секретный текст",
  "passphrase": "пароль123",
  "ttl": 3600
}
```

**Ответ:**
```json
{
  "secret_key": "abc123xyz",
  "message": "Secret created",
  "expires_at": "2024-01-01T12:00:00"
}
```

### Чтение секрета (одноразовое)
```http
GET /api/v1/secrets/{secret_key}?passphrase=пароль123
```

**Ответ:**
```json
{
  "secret": "Мой секретный текст",
  "is_viewed": true,
  "expires_at": "2024-01-01T12:00:00"
}
```

### Проверка здоровья
```http
GET /api/v1/health
```

### Получение токена (для совместимости)
```http
POST /api/v1/token
```

## 📖 Документация API

После запуска приложения доступна интерактивная документация:
- Swagger UI: http://localhost:8000/docs
- ReDoc: http://localhost:8000/redoc

## 🔒 Безопасность

- Все секреты шифруются перед сохранением в базу данных
- Секреты автоматически удаляются после первого прочтения
- Поддержка TTL (время жизни секрета)
- Проверка пароля перед расшифровкой

## ⚙️ Конфигурация

Основные настройки в файле `.env`:

- `MONGODB_URL` - URL подключения к MongoDB
- `SECRET_KEY` - секретный ключ для JWT (измените в продакшене!)
- `ENCRYPTION_KEY` - ключ шифрования (измените в продакшене!)
- `TTL` - время жизни секрета в секундах (по умолчанию 86400 = 24 часа)

## 🧪 Тестирование

```bash
# Запустить тесты
python tests/test_api.py
```

## 📝 Структура проекта

```
key_manager/
├── src/
│   ├── main.py          # Точка входа приложения
│   ├── routes.py        # API endpoints
│   ├── database.py     # Работа с MongoDB
│   ├── encryption.py   # Шифрование данных
│   ├── config.py       # Конфигурация
│   ├── auth.py         # JWT аутентификация
│   └── models.py       # Модели данных
├── tests/              # Тесты
├── docker-compose.yml  # Docker Compose конфигурация
├── Dockerfile          # Docker образ
├── requirements.txt    # Зависимости Python
└── README.md          # Документация
```

## 🤝 Вклад в проект

1. Fork проекта
2. Создайте ветку для новой функции (`git checkout -b feature/AmazingFeature`)
3. Commit изменения (`git commit -m 'Add some AmazingFeature'`)
4. Push в ветку (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

## 📄 Лицензия

Этот проект распространяется под лицензией MIT.

## 👤 Автор

Ваше имя

---

⭐ Если проект был полезен, поставьте звезду!